#include <iostream>
#define MAX 100000
int main(){
  std::cin.tie(nullptr);
  std::ios_base::sync_with_stdio(false);
  
  char* base = (char*)malloc(MAX * sizeof(char));
  register char c;
  std::cin>>base;
  while( (c = getchar()) != 10 ){
    
  }
}